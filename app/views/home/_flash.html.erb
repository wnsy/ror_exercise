<% flash.each do |name, msg| %> <!--  name of the flash, and its msg -->
<div class="alert alert-warning" role="alert">
  <button class="close" data-dismiss="alert">
    <i class="glyphicon glyphicon-remove"></i>
  </button>
  <%= content_tag(:div, msg) %>
</div>
<% end %>
